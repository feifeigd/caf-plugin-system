; CAF 配置文件
; 用法: ./app --config-file=app.ini

[caf-plugin-system]
; 入口插件：只声明顶层插件，系统会自动解析依赖并拓扑排序加载
entry-plugins=["BusinessPlugin"]

; 关闭顺序（可选），留空则使用依赖反序
; shutdown-order=["BusinessPlugin", "LoggerPlugin"]
